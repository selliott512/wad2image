Scripts that are run directly, such as wad2image.py, are here. The path can be
updated to include this directory. Note that by default wad2image depends on
files under the top directory (".." from here), so wad2image.py should not be
copied out by itself.
